import 'package:flutter/material.dart';

const borderRadius8 = BorderRadius.all(Radius.circular(8));

const borderRadius6 = BorderRadius.all(Radius.circular(6));

const edgeInsetsH10V8 = EdgeInsets.symmetric(horizontal: 10, vertical: 8);
const edgeInsetsH8V4 = EdgeInsets.symmetric(horizontal: 8, vertical: 4);

const edgeInsets8 = EdgeInsets.all(8.0);

const normalDivider = Divider(height: 1, color: Color(0xFFECECEC));
